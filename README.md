# Генератор сертификатов с QR-кодами и рассылкой 📜🔗

Краткое описание

- Этот проект генерирует PDF-сертификаты с QR-кодами, сохраняет их в `certificates/`, сохраняет изображения QR-кодов в `qr_codes/` и формирует CSV-отчёт.
- Также реализована возможность отправки сертификатов по email через SMTP.
- Есть консольный интерфейс и простая GUI-опция (`python main.py --gui`).

Быстрый старт ✅

1. Создайте виртуальное окружение и активируйте его:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

2. Установите зависимости:

```powershell
pip install -r requirements.txt
```

3. Настройте переменные окружения (или создайте файл `.env` в корне проекта) с параметрами SMTP и опциями (пример):

```
SENDER_EMAIL=you@example.com
SENDER_PASSWORD=your_app_password_or_token
SMTP_SERVER=smtp.mail.ru
SMTP_PORT=587
FONT_PATH=arial.ttf
FONT_NAME=Arial
```

Важное: никогда не храните пароли в публичных репозиториях. Используйте Personal Access Token или app password.

4. Подготовьте CSV-файл с участниками или используйте тестовые/случайные данные. Ожидаемые колонки для CSV:

- `Имя` (обязательно)
- `Фамилия` (обязательно)
- `Email` (обязательно)
- опционально: `Отчество`, `Курс`, `Часы`, `Дата_завершения`

По умолчанию программа ожидает `participants.csv` в проекте (путь можно изменить в `main.py`).

Запуск 🔧

- Консольный режим:

```powershell
python main.py
```

- GUI режим:

```powershell
python main.py --gui
```

Что делает программа

- Создаёт папки `certificates/`, `qr_codes/` и `templates/` (если их нет).
- Для каждого участника генерирует уникальный ID сертификата, QR-код с URL верификации, создаёт PDF из HTML-шаблона и сохраняет файл.
- Отправляет сертификаты на email (если SMTP доступен).
- Формирует `report.csv` с результатами.

Зависимости и примечания ⚠️

- Python 3.10+
- В `requirements.txt` перечислены Python-пакеты: `jinja2`, `qrcode`, `weasyprint`, `pandas`, `Pillow`.
- WeasyPrint требует дополнительных **системных библиотек** (Cairo, Pango, GDK-PixBuf) на Windows — при проблемах с установкой обратитесь к документации WeasyPrint.

Советы 💡

- Для разработки используйте тестовые данные (`main.py` → выбор 2) или сгенерируйте случайных участников (выбор 3).
- Если файлы сертификатов будут слишком большими, рассмотрите использование Git LFS.

Контакты / Поддержка

- Для вопросов по запуску оставьте issue в репозитории или опишите проблему прямо здесь.
